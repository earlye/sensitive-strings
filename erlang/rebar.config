%% Rebar3 configuration

{erl_opts, [debug_info, warnings_as_errors]}.

{deps, []}.

{project_plugins, [rebar3_ex_doc]}.

{ex_doc, [
    {extras, ["README.md"]},
    {main, "readme"},
    {source_url, "https://github.com/earlye/sensitive-strings"}
]}.

{profiles, [
    {test, [
        {erl_opts, [debug_info, nowarn_export_all]},
        {deps, []}
    ]}
]}.

